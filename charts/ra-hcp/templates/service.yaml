apiVersion: v1
kind: Service
metadata:
  name: {{ include "ra-hcp.fullname" . }}
  labels:
    {{- include "ra-hcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ra-hcp.selectorLabels" . | nindent 4 }}
