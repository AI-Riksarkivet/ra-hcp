# Backend API URL
BACKEND_URL=http://127.0.0.1:8000

# HCP domain (used for NFS mount instructions)
HCP_DOMAIN=

# Cookie secure flag — defaults to true in production, false in dev
# Set to "false" if production runs behind HTTP (no TLS)
COOKIE_SECURE=
