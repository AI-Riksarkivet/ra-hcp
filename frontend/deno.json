{
  "nodeModulesDir": "auto",
  "tasks": {
    "dev": "deno run -A npm:vite dev",
    "build": "deno run -A npm:vite build",
    "preview": "deno run -A npm:vite preview",
    "sync": "node node_modules/@sveltejs/kit/svelte-kit.js sync",
    "check": "node node_modules/@sveltejs/kit/svelte-kit.js sync && deno run -A npm:svelte-check --tsconfig ./tsconfig.json"
  }
}
