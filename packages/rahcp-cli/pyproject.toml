[project]
name = "rahcp-cli"
version = "0.1.0"
description = "CLI for HCP Unified API"
requires-python = ">=3.13"
dependencies = ["rahcp-client", "typer>=0.15", "rich>=13.0", "pyyaml>=6.0"]

[project.scripts]
rahcp = "rahcp_cli.main:app"

[tool.uv.sources]
rahcp-client = { workspace = true }

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
